---
galaxy_info:
  role_name: aws_lambda_dependency_layer
  description: >
    Build and deploy an AWS Lambda layer bundled with the dependencies
    for a given runtime.
  author: Diógenes Oliveira

  license: MIT

  min_ansible_version: 1.2

  platforms:
    - name: Linux
      versions:
        - all

  galaxy_tags:
    - lambda
    - aws

dependencies: []
