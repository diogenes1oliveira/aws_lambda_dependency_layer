---
mode: "deploy"
# - "check": don't build, just check the state
# - "build": just build the layer code
# - "deploy": build and deploy

runtime: "ruby2.5"
# valid AWS Lambda runtime

state: "present"
# 'absent' or 'present'
